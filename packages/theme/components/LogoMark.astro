---
const { siteConfig } = Astro.props;
const alt = siteConfig?.name ?? "FinanceRookie";
---

<a href="/" class="logo-lockup" aria-label={alt}>
  <img
    src="/logo1.svg"
    alt={alt}
    class="logo-img"
    loading="eager"
    decoding="async"
  />
</a>
